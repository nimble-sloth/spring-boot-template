spring.application.name=template

# --- H2 in-memory (dev default) ---
spring.datasource.url=jdbc:h2:mem:tmpl;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=none

# Expose actuator endpoints
management.endpoints.web.exposure.include=*

# Swagger/OpenAPI
springdoc.api-docs.enabled=true
springdoc.swagger-ui.path=/swagger-ui.html

# SMB credentials (used by SmbFileManagerImpl)
smb.username=
smb.password=

spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.mongo.MongoAutoConfiguration,org.springframework.boot.autoconfigure.data.mongo.MongoDataAutoConfiguration

# (Optional) Email/JMS — fill if you use them
# spring.mail.host=smtp.example.com
# spring.mail.port=587
# spring.mail.username=youruser
# spring.mail.password=yourpass
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true

# spring.activemq.broker-url=tcp://localhost:61616
# spring.jms.template.default-destination=notifications
